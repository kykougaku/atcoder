#include <bits/stdc++.h>

using namespace std;

int main (){
    int N,K;
    cin >> N >> K;
    vector<int> a[N];
    for(int i=0;i<N;i++){
        int tmp;
        cin >> tmp;
        a[i].push_back(tmp);
    }

    return 0;
}