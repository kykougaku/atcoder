#include <bits/stdc++.h>

using namespace std;

int main (){
    int n;
    string s;
    cin >> n >> s;
    vector<int> c(n);
    for(int i = 0; i < n; i++){
        cin >> c.at(i);
    }
    return 0;
}