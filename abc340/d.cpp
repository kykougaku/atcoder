#include <bits/stdc++.h>

using namespace std;

int main (){
    int64_t n;
    cin >> n;
    vector<int64_t> a(n), b(n), x(n);
    for(int64_t i = 0; i < n-1; i++) cin >> a.at(i) >> b.at(i)>> x.at(i);

    queue<int64_t> que;
    vector<int64_t> ans(n+1, 1000000000);
    while(!que.empty()){
        int64_t now = que.front();
        que.pop();
        for(int64_t i = 1; i < n+1; i++){
            }
            }
        }
    }

    return 0;
}